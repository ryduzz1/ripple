{
  "name": "ripple",
  "version": "0.1.0",
  "private": true,
  "description": "After Effects CEP extension for faster editorial timing, trimming, snapping, and timeline cleanup.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "node scripts/build.mjs --clean",
    "install:cep": "node scripts/install-cep.mjs",
    "package": "node scripts/package.mjs",
    "zxp": "node scripts/zxp.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
